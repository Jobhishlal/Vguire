<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Offer Add</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        body {
            background: linear-gradient(45deg, #121212, #1a1a1a);
            color: #fff;
            font-family: 'Arial', sans-serif;
        }
        .container {
            margin-top: 50px;
            padding: 30px;
            background-color: rgba(0, 0, 0, 0.75);
            border-radius: 15px;
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.3);
        }
        h1 {
            text-align: center;
            color: #ffb900;
            font-size: 2.5rem;
            margin-bottom: 30px;
            text-transform: uppercase;
        }
        .form-label {
            font-size: 1.1rem;
            color: #bbb;
        }
        .form-control {
            background-color: #333;
            border: 1px solid #444;
            color: #fff;
            border-radius: 8px;
            padding: 15px;
        }
        .form-control:focus {
            border-color: #ffb900;
            box-shadow: 0 0 10px rgba(255, 185, 0, 0.6);
        }
        .btn-submit {
            background-color: #ffb900;
            color: #000;
            border-radius: 8px;
            padding: 12px;
            width: 100%;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        .btn-submit:hover {
            background-color: #ff8f00;
            box-shadow: 0 5px 15px rgba(255, 143, 0, 0.5);
        }
        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #ffb900;
            font-size: 1.2rem;
            transition: color 0.3s ease;
        }
        .back-link:hover {
            color: #fff;
        }
        .alert {
            border-radius: 5px;
        }
        .alert-success {
            background-color: #28a745;
            color: #fff;
            border: none;
        }
        .alert-error {
            background-color: #d33;
            color: #fff;
            border: none;
        }
        .alert-title {
            font-weight: bold;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Add Offer to Product</h1>

        <% if (success && success.length > 0) { %>
            <div class="alert alert-success">
                <span class="alert-title">Success!</span> <%= success %>
            </div>
        <% } %>
        
     
        <% if (error && error.length > 0) { %>
            <div class="alert alert-error">
                <span class="alert-title">Error!</span> <%= error %>
            </div>
        <% } %>

        <form action="/admin/product/<%= product._id %>/offer" method="POST">
            <div class="mb-4">
                <label for="discountPercentage" class="form-label">Discount Percentage:</label>
                <input type="number" id="discountPercentage" name="discountPercentage" class="form-control" min="1" max="100" required>
            </div>
            <button type="submit" class="btn-submit">Apply Offer</button>
        </form>

        <a href="/admin/product" class="back-link">Back to Products</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
   
      

</body>
</html>
